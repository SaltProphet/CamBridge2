{
  "name": "cambridge",
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=20.x"
  },
  "scripts": {
    "dev": "npx http-server -p 3000 -g",
    "build": "echo 'Static site - files in root are served as-is'",
    "test": "node test.js",
    "test:api": "node --test api/**/*.test.js"
  },
  "dependencies": {
    "@vercel/postgres": "^0.12.0",
    "bcryptjs": "^2.4.3",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.1.2",
    "resend": "^3.2.0"
  },
  "devDependencies": {}
}