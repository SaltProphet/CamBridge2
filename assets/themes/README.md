# After Hours Theme Assets

This directory is reserved for future theme customization files.

Theme settings are currently stored in localStorage as CSS variables:
- `--accent` - Primary accent color
- `--glass-opacity` - Widget background opacity
